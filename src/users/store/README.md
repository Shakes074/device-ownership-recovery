# Store Portal

Shared documentation for store-level flows. Use subfolders for each persona:

- `store-admin/` for administrators
- `store-staff/` for frontline employees

Feature code should live under `src/features/store/`.
